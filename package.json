{
  "name": "countdownreminder",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --win portable",
    "package": "npx electron-packager . countdownreminder --platform=win32 --arch=x64 --icon=icon.ico --out=dist --asar --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "build": {
    "appId": "com.subscription.reminder",
    "productName": "订阅提醒",
    "win": {
      "target": [
        "portable"
      ]
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "electron": "^41.3.0",
    "electron-builder": "^26.8.1",
    "png-to-ico": "^3.0.1"
  }
}
